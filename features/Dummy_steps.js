const {
  Given,
  When,
  Then,
  Fusion,
  // eslint-disable-next-line import/no-extraneous-dependencies
} = require('jest-cucumber-fusion');

Given('write given statement here', () => {});

When('write when statement here', () => {});

Then('write then statement here', () => {});

Fusion('Dummy.feature');
