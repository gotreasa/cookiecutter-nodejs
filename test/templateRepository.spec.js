const TemplateRepository = require('../src/templateRepository.js');

describe('Name of the group', () => {
  test('should ...', () => {
    const templateRepository = new TemplateRepository();
    expect(templateRepository).toBeDefined();
  });
});
