openapi: '3.0.3'
info:
  title: Template NodeJS & Express API
  description: Template NodeJS & Express API for testing.
  version: 1.0.0
servers:
  - url: http://localhost:9080
  - url: https://template-nodejs-app.wdc1a.ciocloud.nonprod.intranet.ibm.com
paths:
  /api/v1/dummy:
    get:
      summary: Returns a list of users.
      description: List of users on the system.
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
